---
import ProfesorLayout from '../../../../layouts/ProfesorLayout.astro';
import { PlanDetail } from '@/components/organisms/profesor/planes/PlanDetail.tsx';
import { createSupabaseServerClient } from "@/lib/supabase-ssr";
import { planesCopy } from "@/data/es/profesor/planes";

const { id } = Astro.params;
const supabase = createSupabaseServerClient(Astro);

// 1. Auth check
const { data: { user } } = await supabase.auth.getUser();
if (!user) return Astro.redirect("/login");

// 2. Fetch plan completo con rutinas, ejercicios y alumnos activos
const { data: plan, error } = await supabase
  .from("planes")
  .select(`
    id,
    nombre,
    duracion_semanas,
    frecuencia_semanal,
    created_at,
    rutinas_diarias (
      id,
      dia_numero,
      nombre_dia,
      orden,
      ejercicios_plan (
        id,
        orden,
        series,
        reps_target,
        descanso_seg,
        biblioteca_ejercicios (
          id,
          nombre,
          media_url
        )
      )
    ),
    alumnos (
      id,
      nombre,
      email,
      estado
    )
  `)
  .eq("id", id)
  .eq("profesor_id", user.id)
  .single();

if (error || !plan) {
  return Astro.redirect("/profesor/planes?error=not-found");
}

// 3. Ordenar rutinas y ejercicios por orden
const sortedRutinas = (plan.rutinas_diarias || [])
  .sort((a: any, b: any) => a.dia_numero - b.dia_numero)
  .map((r: any) => ({
    ...r,
    ejercicios_plan: (r.ejercicios_plan || []).sort((a: any, b: any) => a.orden - b.orden),
  }));

const c = planesCopy.detail;
---

<ProfesorLayout title={`${plan.nombre} | MiGym`}>
  <div class="px-5 py-6 md:py-12 md:px-10 max-w-6xl mx-auto space-y-8 animate-in fade-in duration-500">

    {/* BREADCRUMB */}
    <nav aria-label="Migas de pan">
      <a
        href="/profesor/planes"
        class="inline-flex items-center gap-2 text-sm font-black text-zinc-400 hover:text-zinc-950 dark:hover:text-zinc-100 uppercase tracking-widest transition-colors duration-200"
      >
        {c.actions.back}
      </a>
    </nav>

    {/* CABECERA VITAL */}
    <section class="animate-in slide-in-from-top-4 duration-500">
      <PlanDetail client:load plan={{
        id: plan.id,
        nombre: plan.nombre,
        duracion_semanas: plan.duracion_semanas,
        frecuencia_semanal: plan.frecuencia_semanal,
        created_at: plan.created_at,
        rutinas: sortedRutinas as any,
        alumnos: (plan.alumnos || []) as any,
      }} />
    </section>

  </div>
</ProfesorLayout>
